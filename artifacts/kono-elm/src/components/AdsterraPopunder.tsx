'use client';

/**
 * AdsterraPopunder component
 * Previously managed the display of Popunder advertisement.
 * Deactivated as requested to replace all existing ads with the new ad snippet.
 */
export default function AdsterraPopunder() {
  return null;
}
